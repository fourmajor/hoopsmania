# Hoops Mania Web (Next.js)

Minimal Next.js frontend for the Hoops Mania homepage.

## Run locally

From this `web/` directory:

```bash
npm install
npm run dev
```

Then open <http://localhost:3000>.

## Build check

```bash
npm run build
```
